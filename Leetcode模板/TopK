# [215. 数组中的第K个最大元素]

## 调库



## 借助 partition 操作定位到最终排定以后索引为 `len - k` 的那个元素（特别注意：随机化切分元素）

partition函数的作用：可以寻找到一个元素在数组顺序的位置index，并且该元素左边均小于该元素，该元素右边均大于该元素。

### 思路: 先开始使用一个partition函数，找到一个index，判断这个index在要寻找的位置的左边还是右边。进而缩减区间，从而达到分而治之。

## 优先队列（贪心算法的应用）